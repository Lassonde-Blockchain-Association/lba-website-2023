/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
};

module.exports = nextConfig;

module.exports = {
  images: {
    domains: ["images.pexels.com", "https://user-images.githubusercontent.com", "media.licdn.com", "th.bing.com"],
  },
};
